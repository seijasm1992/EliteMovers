---
import { PUBLIC_GEOAPIFY_API_KEY } from "astro:env/client";
import Footer from "../components/layout/Footer.astro";
import Navbar from "../components/layout/Navbar.astro";
import QuoteForm from "../components/react/QuoteForm";
import Icon from "../components/ui/Icon.astro";
import { quoteForm } from "../data/quoteForm";
import { site } from "../data/site";
import BaseLayout from "../layouts/BaseLayout.astro";

const emailHref = `mailto:${site.email}`;
---

<BaseLayout
  title={`Get a Free Moving Quote | ${site.name}`}
  description="Tell us about your move and receive a clear, personalized moving estimate from ProElite Movers."
>
  <Navbar />
  <main id="main-content">
    <section id="top" class="quote-page relative flex min-h-[100svh] items-center overflow-hidden bg-brand-surface px-4 pb-14 pt-28 sm:px-6 sm:pb-20 sm:pt-32 lg:px-8 lg:py-32">
      <div class="quote-page__glow quote-page__glow--gold" aria-hidden="true"></div>
      <div class="quote-page__glow quote-page__glow--green" aria-hidden="true"></div>

      <div class="relative mx-auto w-full max-w-6xl">
        <div class="grid overflow-hidden rounded-[2rem] border border-white/80 bg-offwhite shadow-bubble lg:grid-cols-[0.85fr_1.15fr]">
          <div class="flex flex-col justify-center p-8 sm:p-12 lg:p-16" data-scroll-reveal>
            <p class="eyebrow text-green">We're here to help</p>
            <h1 class="mt-5 max-w-md text-5xl leading-[0.9] tracking-[-0.045em] text-text-strong sm:text-6xl lg:text-7xl">
              Let's Plan<br />Your Move
            </h1>
            <p class="mt-6 max-w-md text-base leading-relaxed text-text-subtle sm:text-lg">
              Tell us about your move and we'll prepare a clear, personalized estimate—no pressure and no hidden surprises.
            </p>

            <dl class="mt-10 grid gap-5 sm:mt-12">
              <div class="flex items-center gap-4">
                <div class="flex h-11 w-11 shrink-0 items-center justify-center rounded-full bg-green text-white shadow-soft">
                  <Icon name="email" class="h-5 w-5" />
                </div>
                <div>
                  <dt class="font-accent text-xs font-semibold uppercase tracking-[0.15em] text-text-subtle">Email</dt>
                  <dd class="mt-1">
                    <a href={emailHref} class="font-accent text-sm font-bold text-text-strong transition-colors hover:text-green">
                      {site.email}
                    </a>
                  </dd>
                </div>
              </div>
              <div class="flex items-center gap-4">
                <div class="flex h-11 w-11 shrink-0 items-center justify-center rounded-full bg-brand-yellow text-brand-primary shadow-soft">
                  <Icon name="phone" class="h-5 w-5" />
                </div>
                <div>
                  <dt class="font-accent text-xs font-semibold uppercase tracking-[0.15em] text-text-subtle">Phone number</dt>
                  <dd class="mt-1">
                    <a href={site.phoneHref} class="font-accent text-sm font-bold text-text-strong transition-colors hover:text-green">
                      {site.phone}
                    </a>
                  </dd>
                </div>
              </div>
            </dl>
          </div>

          <div class="flex items-center bg-white/40 p-4 sm:p-8 lg:p-12" data-scroll-reveal style="--reveal-delay: 100ms">
            <div class="mx-auto w-full max-w-[40rem]">
              <QuoteForm client:load variant="page" content={quoteForm} geoapifyApiKey={PUBLIC_GEOAPIFY_API_KEY} />
            </div>
          </div>
        </div>
      </div>
    </section>
  </main>
  <Footer />
</BaseLayout>

<style>
  .quote-page::before {
    position: absolute;
    inset: 0;
    background:
      linear-gradient(115deg, rgb(255 255 255 / 0.72), transparent 48%),
      radial-gradient(circle at 84% 14%, rgb(255 216 77 / 0.15), transparent 27%),
      radial-gradient(circle at 16% 88%, rgb(0 112 76 / 0.08), transparent 25%);
    content: "";
    pointer-events: none;
  }

  .quote-page__glow {
    position: absolute;
    border-radius: 9999px;
    filter: blur(12px);
    opacity: 0.28;
    pointer-events: none;
  }

  .quote-page__glow--gold {
    top: 17%;
    right: -8rem;
    width: 18rem;
    height: 18rem;
    background: var(--color-brand-yellow);
  }

  .quote-page__glow--green {
    bottom: -8rem;
    left: -7rem;
    width: 15rem;
    height: 15rem;
    background: var(--color-green);
  }

  @media (prefers-reduced-motion: reduce) {
    .quote-page__glow {
      filter: none;
    }
  }
</style>
