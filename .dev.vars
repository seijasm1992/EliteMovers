# Cloudflare local dev — Wrangler inyecta este archivo en runtime con `npm run dev`
# NUNCA commitear este archivo — ya está en .gitignore
# https://developers.cloudflare.com/workers/wrangler/configuration/#environment-variables

# ── Resend ──────────────────────────────────────────────────────────────────
RESEND_API_KEY=re_MVQfEoFD_9Xzh785FSX5oSA1xePxQ1kbE

# Destinatario de los formularios
QUOTE_TO_EMAIL=info@proelitemovers.com

# Sender verificado en Resend (el dominio proelitemovers.com debe estar verificado)
QUOTE_FROM_EMAIL=ProElite Movers <quotes@proelitemovers.com>

# ── Cloudflare Turnstile ─────────────────────────────────────────────────────
PUBLIC_TURNSTILE_SITE_KEY=0x4AAAAAADlRCEwUqwhp5VPj
TURNSTILE_SECRET_KEY=0x4AAAAAADlRCKU2KHxR8Yr93v4fLggHH3A

# false = skip verificación en dev local  |  true (default) = siempre verificar
# En producción NO definir esta variable (defaults a true automáticamente)
ENABLE_TURNSTILE=false
